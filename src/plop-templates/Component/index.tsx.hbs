import React from 'react';

import Styles{{pascalCase name}} from './styles';

export interface {{pascalCase name}}Props {
  children?: React.ReactNode;
}

const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = ({ children }) => {
  //Some Logic
  return (
    <Styles{{pascalCase name}} className="{{classNameComponent type}}{{camelCase name}}">
      {children}
    </Styles{{pascalCase name}}>
  )
};

{{pascalCase name}}.defaultProps = {
  children: undefined,
};

export default {{pascalCase name}};

