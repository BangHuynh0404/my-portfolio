{{!-- import styled, { css } from "styled-components"; --}}
import styled from "@emotion/styled"
import { css } from "@emotion/react"
import { {{pascalCase name}}Props } from ".";

interface Styled{{pascalCase name}}Props extends {{pascalCase name}}Props {
   // Custom Props
};

const Styled{{pascalCase name}} = styled.div`${(props: Styled{{pascalCase name}}Props) => {
   let root = "{{classNameComponent type}}{{camelCase name}}";
   return css`
   
   `;
}}
`;

export default Styled{{pascalCase name}};
