import styled from "@emotion/styled";
import { css } from "@emotion/react";

const StylesMyGithub = styled.div`
   
   main {
      padding: 50px;
      .mantine-Card-root {
         display: flex;
         flex-direction: column;
         justify-content: space-between;
         height: 400px;
      }
      a {
         text-decoration: none;
      }
   }

`;

export default StylesMyGithub;
